import 'package:flutter/material.dart';

class AppColors {
  static Color? primaryBlue = Colors.blueAccent[700];
  static const Color green = Color(0xff42B72A);
  static Color? inputBackground = Colors.grey[200];
}
